/* eslint-disable import/prefer-default-export */
import If from './if.vue';
import ElseIf from './else-if.vue';
import Else from './else.vue';
import For from './for.vue';

export default {
    If,
    ElseIf,
    Else,
    For
};
